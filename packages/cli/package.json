{
  "name": "@honk/cli",
  "version": "0.7.0",
  "description": "Run Honk Core and connect through Tailscale.",
  "license": "MIT",
  "bin": {
    "honk": "./dist/bin.mjs"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun src/bin.ts",
    "build": "tsdown src/bin.ts --format esm --out-dir dist --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.83.0",
    "@earendil-works/pi-ai": "0.83.0",
    "@effect/platform-node": "catalog:",
    "@modelcontextprotocol/sdk": "^1.25.2",
    "effect": "catalog:",
    "toqr": "0.1.1",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@honk/core": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=24"
  }
}
