#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

if [[ ! -d node_modules ]]; then
  printf '[orb resume] node_modules is missing; rerun .agents/setup\n' >&2
  exit 1
fi

printf '[orb resume] ready\n'
